<h1>Item List</h1>
<% if current_user.admin_flag? %>
  <%= link_to 'Add item', '/item/add' %>
<% end %>
<table>
  <tr>
    <th>Name</th>
    <th>Value</th>
    <% if current_user.admin_flag? %>
    <th></th>
    <th></th>
    <% end %>
  </tr>
  <% @items.each do |item| %>
    <tr>
      <td style="width:200px;">
        <div style="width:200px; overflow-x:scroll;">
          <%= item.name %>
        </div>
      </td>
      <td style="width:200px;">
        <div style="width:200px; overflow-x:scroll;">
          <%= item.value %>
        </div>
      </td>
      <% if current_user.admin_flag? %>
      <td><%= link_to 'update', { :controller => :items, :action => :update, :id => item.id } %></td>
      <td><%= link_to 'delete', { :controller => :items, :action => :delete, :id => item.id } %></td>
      <% end %>
    </tr>
  <% end %>
</table>
